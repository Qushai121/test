export type RatingProps = {
  // determine how many star is activejust take number no float or negatif
  rating: number;
  // determine how many star or rate can be, example : 7/10 pass 10 to maxRating pass 7 to rating
  maxRating: number;
};
